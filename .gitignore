# Rust build output
/target
# syncode-ui 是独立 workspace (gpui spike), 其 target 在自己目录下, 单独忽略。
/crates/syncode-ui/target
**/*.rs.bk
*.pdb

# 本项目是应用 (含二进制), Cargo.lock 应提交 —— 故不忽略 Cargo.lock。

# Secrets / 本地环境
.env
*.env

# 编辑器 / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Claude Code 工具产物 (workflow 脚手架脚本等), 本地研究用, 不进仓库。
.cc-*.workflow.js

# 被借鉴的第三方参考源码: 仅本地参考工具设计 (架构 §10), 不随开源仓库分发。
/claude-code-main/

# private-docs: 设计 / 策略文档, 仅本地保留, 不进公开仓库。
/private-docs/
